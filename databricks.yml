# This is a Databricks asset bundle definition for adt_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: adt_project
  uuid: 64b760b8-0a90-4d70-ab21-0c1b4288e1b8

include:
  - resources/*.yml
  - resources/*/*.yml


sync:
  exclude:
    - env_dbw/**
    - .git/**
    - __pycache__/**

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-475772394194112.12.azuredatabricks.net

  test:
    mode: production
    workspace:
      host: https://adb-3219440406780807.7.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/terje.farup@soprasteria.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: terje.farup@soprasteria.com
        level: CAN_MANAGE
      - service_principal_name: sp-dbw-adt-test
        level: CAN_MANAGE
      - group_name: users
        level: CAN_MANAGE
